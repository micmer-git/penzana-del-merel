Proceed end-to-end now. Super-thinking mode.

Fixed decisions:
- Purpose: all of the above (promotion + heritage + practical info + digital ordering)
- Tech stack: Astro static site on GitHub Pages
- Language: bilingual IT/EN
- Repo name: micmer/penzana-del-merel

Primary objective:
Create and publish a polished bilingual website for Penzana del Merel / Orezzo with SEO, sleek menu section, and QR-table ordering flow.

Mandatory factual sources:
1) Web research of current site/authoritative info.
2) Local primary source file:
   C:\Users\micme\Desktop\micmer\penzana_menu_story_source_2026.md
3) If data is missing, never invent; add explicit placeholder + TODO.

Core website sections:
- Home
- Storia / History
- Famiglia / Family
- Territorio / Area
- Menù
- Galleria
- Contatti

Design system:
- Typographic, rural, alpine identity
- Palette: deep alpine green/blue + stone neutrals + subtle light-violet accent
- Premium readable typography and sleek navigation

SEO + accessibility:
- semantic HTML
- per-page title/meta description
- OpenGraph/Twitter cards
- sitemap.xml + robots.txt
- schema.org structured data where valid
- canonical URLs
- accessibility baseline (contrast, keyboard, landmarks, alt text)

NEW FEATURE — QR table ordering to WhatsApp (mandatory):
Build a client-side ordering flow suitable for GitHub Pages:
1) Unique table association
   - Generate unique QR codes for tables (at least table-01 to table-20).
   - Each QR must open the site with query param for table id, e.g. ?table=table-01
2) Ordering session
   - User sees menu items (from source), can add quantities, notes, and submit.
   - Session stores table id and selected items in local state.
3) WhatsApp message generation
   - On submit, generate structured order message including:
     - table id
     - timestamp
     - selected items + qty + price subtotal
     - optional notes
     - total estimate
   - Trigger WhatsApp send via wa.me deep link to a configurable WhatsApp number.
   - Keep number configurable in a central config file.
4) UX details
   - Mobile-first ordering UI
   - clear success/confirm step before opening WhatsApp
   - bilingual labels (IT/EN)
5) Deliverables for QR
   - generate printable QR assets (PNG and/or SVG) per table in /public/qr/
   - provide a simple printable sheet page for all table QR codes.

Repo + deploy:
- Create repo under micmer, push code, enable GitHub Pages live.
- Add README with setup/edit instructions and ordering config instructions.
- Include sources.md with factual source list and confidence notes.

Final report (strict):
- repo URL
- live URL
- architecture summary
- SEO checklist completed
- ordering flow summary
- where QR files are located + how many generated
- files created
- open TODO placeholders (including WhatsApp number if not provided)

Do full implementation now autonomously.